import React from 'react';
import { render } from '@testing-library/react';
import { CreateCharacterForm } from './CreateCharacterForm';

describe('CreateCharacterForm', () => {
  it('should trigger submit when form is submitted', () => {

  })
})